<p id="notice"><%= notice %></p>

<h1>Listing Categoria Ings</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categoria_ings.each do |categoria_ing| %>
      <tr>
        <td><%= categoria_ing.nombre %></td>
        <td><%= link_to 'Show', categoria_ing %></td>
        <td><%= link_to 'Edit', edit_categoria_ing_path(categoria_ing) %></td>
        <td><%= link_to 'Destroy', categoria_ing, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Categoria ing', new_categoria_ing_path %>
