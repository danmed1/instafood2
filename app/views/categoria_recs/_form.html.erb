<%= form_for(@categoria_rec) do |f| %>
  <% if @categoria_rec.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categoria_rec.errors.count, "error") %> prohibited this categoria_rec from being saved:</h2>

      <ul>
      <% @categoria_rec.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
